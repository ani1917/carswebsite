package com.qa.carmax.pages;

import org.openqa.selenium.WebDriver;

import com.qa.carmax.objects.SignupPage;

public class SingupPageActions {
	WebDriver driver;
	public void SingupPageActionse(WebDriver driver) {
		this.driver = driver;
	}

	/*public void fnameregistration() {
		
		driver.findElement(SignupPage.fname).sendKeys("");
		driver.findElement(SignupPage.lname).sendKeys("");
		driver.findElement(SignupPage.email).sendKeys("");
		driver.findElement(SignupPage.mobile).sendKeys("");
		driver.findElement(SignupPage.password).sendKeys("");*/
	}
	

